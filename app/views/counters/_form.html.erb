<%= form_with(model: counter, local: true) do |form| %>
  <% if counter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(counter.errors.count, "error") %> prohibited this counter from being saved:</h2>

      <ul>
        <% counter.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= form.label :counter_ends %>
    <%= form.datetime_select :counter_ends %>
  </div>

  <div class="field">
    <%= form.label :counter_text %>
    <%= form.text_field :counter_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
